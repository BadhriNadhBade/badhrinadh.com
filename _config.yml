title:            Badhri Nadh
description:      Badhri Nadh's home page on the World Wide Web.
url:              https://badhrinadh.com
permalink:        /posts/:title
timezone:         America/Chicago
sass:
  sass_dir: assets
  style: :compressed
collections:
  notes:
    output: true
  pages:
    output: true
# Ships with the github-pages gem but is off by default, so it has to be named
# here. Generates redirect pages at build time from `redirect_from` front
# matter — see index.html, which absorbs the URLs of the removed photo gallery.
plugins:
  - jekyll-redirect-from
i18n:
  en-US:
    accessibility: Accessibility statement
    source: View source
    colophon: Colophon
    now: Now
# OpenHeart endpoint for the like button. Until you point this at your own
# instance (https://github.com/dddddddddzzzz/OpenHeart), the button renders
# but records nothing.
open_heart_endpoint: ""

markdown: CommonMarkGhPages
commonmark:
  options: ["UNSAFE", "HARDBREAKS"]
  extensions: ["strikethrough", "autolink", "table"]
recent_date: "2026/07/18"
exclude:
  - README.md
  - start
  - Gemfile
  - Gemfile.lock
  - vendor
